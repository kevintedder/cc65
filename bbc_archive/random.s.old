;
; File generated by cc65 v 2.16 - Git N/A
;
	.fopt		compiler,"cc65 v 2.16 - Git N/A"
	.setcpu		"6502"
	.smart		on
	.autoimport	on
	.case		on
	.debuginfo	off
	.importzp	c_sp, sreg, regsave, regbank
	.importzp	tmp1, tmp2, tmp3, tmp4, ptr1, ptr2, ptr3, ptr4
	.macpack	longbranch
	.forceimport	__STARTUP__

	.export		_random, _randomise

.segment	"BSS"
_seed:
	.res	4,$00
	
.segment	"RODATA"
	
; ---------------------------------------------------------------
; void __near__ randomise (long __seed)
; ---------------------------------------------------------------

.segment	"CODE"

.proc	_randomise: near

.segment	"CODE"
;
	jsr     pusheax
;
; if (__seed == 0) {
;
	jsr     ldeax0sp
	jsr     pusheax
	ldx     #$00
	stx     sreg
	stx     sreg+1
	txa
	jsr     toseqeax
	beq     L009B
;
; sd = get_system_time();
;
	jsr     _get_system_time
;
; else {
;
	jmp     L00BA
;
; set _seed;
;
L009B:
	jsr     ldeax0sp
L00BA:
	sta     _seed
	stx     _seed+1
	ldy     sreg
	sty     _seed+2
	ldy     sreg+1
	sty     _seed+3
;
; }
;
	jsr 	_random
	jmp     incsp4

.endproc

; ---------------------------------------------------------------
; long __near__ random (void)
; ---------------------------------------------------------------

.segment	"CODE"

.proc	_random: near

.segment	"CODE"

	ldy		#$00

L0110:
	lda		_seed,y
	cmp		#$00
	bne		L0100

	iny
	cpy		#$04
	bne		L0110
	
;	
;	random seed == 0; Initialise
;
	lda		#$00
	sta		sreg
	sta		sreg+1
	tax
	
	jsr     _randomise
	
;
; seed = (seed * 53) + 17;
;
L0100:
	lda     _seed+3
	sta     sreg+1
	lda     _seed+2
	sta     sreg
	ldx     _seed+1
	lda     _seed
	jsr     pusheax
	ldx     #$00
	lda     #$35
	jsr     tosmul0ax
	ldy     #$11
	jsr     inceaxy
	sta     _seed
	stx     _seed+1
	lda     sreg
	sta     _seed+2
	lda     sreg+1
	and		#$7f			; Mask Msb to force value +ve
	sta     _seed+3
;
; return seed;
;
	lda     _seed+3
	sta     sreg+1
	lda     _seed+2
	sta     sreg
	ldx     _seed+1
	lda     _seed
;
; }
;
	rts

.endproc

